<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.hermes.browser.permission.READ_HISTORY" />
    <!-- Required to see all installed apps on API 30+ -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- User must grant this manually in Settings > Apps > Special app access > Usage access -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <!-- Required for ACTION_DELETE to trigger the system uninstall dialog on API 28+ -->
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Optional: share approximate location with the Claude search provider for location-aware answers -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Optional: notify when a Claude answer is ready while you're not looking at the chat -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required to host app widgets when Deck is the default launcher -->
    <uses-permission android:name="android.permission.BIND_APPWIDGET" />

    <!-- Lets a same-signed sibling app (the Hermes Browser) read the Claude key for its CSS-AI. -->
    <permission
        android:name="com.hermes.deck.permission.READ_SETTINGS"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Deck"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.Deck" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="true"
            android:label="Deck Settings"
            android:theme="@style/Theme.Deck">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.DeckNotificationService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".service.ScreenshotAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.BrowserTabReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.hermes.deck.ACTION_BROWSER_TAB_OPENED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.hermes.deck.ACTION_BROWSER_TAB_FOCUSED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.hermes.deck.ACTION_BROWSER_PAGE_LOADED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.hermes.deck.ACTION_BROWSER_TAB_GONE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.hermes.deck.ACTION_BROWSER_TABS_LIST" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Claude credentials for the same-signed Hermes Browser (custom-CSS AI). Read-only. -->
        <provider
            android:name=".SettingsProvider"
            android:authorities="com.hermes.deck.settings"
            android:exported="true"
            android:readPermission="com.hermes.deck.permission.READ_SETTINGS" />

    </application>

</manifest>
